[package]
name = "common"
version = "0.1.0"
edition = "2024"

[dependencies]
sdk = { path = "../../sdk" }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }

#[lib]
#crate-type = ["dylib", "rlib"]